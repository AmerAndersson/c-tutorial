
```c

  ++k increments the value,
  then returns it.
  k: 2 j : 2


  int k = 1;
  int j = ++k;
  printf("k: %d j: %d\n", k, j);
  printf("\n");

  n: 2 m : 1
  n++ returns the value,
  and then increments it.
  int n = 1;
  int m = n++;
  printf("n: %d m: %d\n", n, m);
```